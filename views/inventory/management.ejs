<!-- Pass on title -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

    <%# messages() %>

<div class="links">
    <a href="/inv/newClassifacation">Add New Classifacation</a>
    <a href="/inv/newVehicle">Add New Vehicle</a>
</div>